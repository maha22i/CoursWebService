import { apiValidationPagination } from './api-validation-pagination';

describe('apiValidationPagination', () => {
  it('should work', () => {
    expect(apiValidationPagination()).toEqual('api-validation-pagination');
  });
});
